# Course Scheduler (Python)

    **When:** Nov 2025  
    **Stack:** Python 3 (standard library)

    ## Overview
    Schedules classes while avoiding time conflicts; stores simple data in a JSON file.

    ## How to run
    ```bash
    python scheduler.py --demo
    ```

    ## What I learned
    - Time conflict detection (interval overlap)
- Basic persistence (JSON)
- Simple CLI ergonomics

    ## Folder structure
    ```text
    (key files only)
    ```